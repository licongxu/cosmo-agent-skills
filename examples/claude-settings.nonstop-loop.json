{
  "_comment": "Optional: merge env into your project's .claude/settings.json if Stop hook blocks are truncated. Hooks ship in the cosmo-agent-skills plugin (hooks/hooks.json) — no copy step required.",
  "env": {
    "CLAUDE_CODE_STOP_HOOK_BLOCK_CAP": "1000000"
  }
}
